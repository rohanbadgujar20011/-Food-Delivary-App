import React from 'react';
import OrderHistory from '../components/OrderHistory';

const Orders = () => {
    return (
        <div>
            <h1>Your Orders</h1>
            <OrderHistory />
        </div>
    );
};

export default Orders;